package com.lokodom.alquilatucoche.ui.theme

import androidx.compose.ui.graphics.Color

// — Primarios ————————————————————————————————
val Primary = Color(0xFF3D7FFF)          // Azul eléctrico
val PrimaryVariant = Color(0xFF2563EB)
val OnPrimary = Color(0xFFFFFFFF)

// — Superficie ————————————————————————————————
val Background = Color(0xFF0A0A0F)       // Negro profundo
val Surface = Color(0xFF13131A)          // Gris muy oscuro
val SurfaceVariant = Color(0xFF1E1E2A)   // Tarjeta
val SurfaceElevated = Color(0xFF252533)  // Tarjeta elevada

// — Texto ─────────────────────────────────────
val OnBackground = Color(0xFFF4F4F6)
val OnSurface = Color(0xFFE8E8ED)
val OnSurfaceVariant = Color(0xFF9898A8)
val OnSurfaceMuted = Color(0xFF5A5A6A)

// — Acento ────────────────────────────────────
val Accent = Color(0xFF3D7FFF)
val AccentGreen = Color(0xFF22C55E)
val AccentAmber = Color(0xFFF59E0B)
val AccentRed = Color(0xFFEF4444)

// — Bordes ────────────────────────────────────
val Border = Color(0xFF2A2A38)
val BorderSubtle = Color(0xFF1E1E2A)